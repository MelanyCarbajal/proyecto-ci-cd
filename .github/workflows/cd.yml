name: CD Placeholder

on:
  workflow_run:
    workflows: ["Java CI"]
    types: [completed]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Deployment step (coming soon)
        run: echo "Deployment will be configured later."
